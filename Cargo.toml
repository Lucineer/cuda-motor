[package]
name = "cuda-motor"
version = "0.1.0"
edition = "2021"
description = "Motor primitives — action execution, movement commands, sequence planning, safety guards, and effort estimation"

[dependencies]
serde = { version = "1", features = ["derive"] }
